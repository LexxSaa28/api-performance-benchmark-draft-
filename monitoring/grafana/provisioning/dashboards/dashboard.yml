apiVersion: 1

providers:
  - name: 'Backend Performance Dashboards'
    orgId: 1
    folder: 'Load Testing'
    folderUid: 'loadtest'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards